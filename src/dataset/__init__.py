from __future__ import annotations


def main() -> int:
    """
    Console entrypoint for the dataset package.

    The dataset package is primarily intended as a library; for examples, see
    `examples/` and `pipeline/`.

    """
    # CLI entrypoint: stdout is the intended output channel.
    print("dataset: no CLI implemented. See `examples/` and `pipeline/`.")  # noqa: T201
    return 0
