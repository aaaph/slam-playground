name: full_batch_profile
dataset: euroc_v101
dataflow:
  template: slam-dataflow.yml
  build: false
visualization:
  sink: file
run:
  mode: batch_fraction
  fraction: 1.0
  autostart_after_ready: true
  stop_after_dataset_done: true
  control_transport: none
