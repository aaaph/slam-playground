name: slam_agent_profile
dataset: euroc_v201
dataflow:
  template: slam-dataflow.yml
  build: false
visualization:
  sink: file
run:
  mode: batch_fraction
  fraction: 0.05
  autostart_after_ready: true
  stop_after_dataset_done: true
  control_transport: none
