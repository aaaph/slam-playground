[project]
name = "vins-rnd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ivan Aphanasyev", email = "nekaktotak2@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
vins-rnd = "vins_rnd:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
