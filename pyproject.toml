[project]
name = "vins-rnd"
version = "0.1.0"
description = "Research and Development of Visual Inertial Navigation Systems"
readme = "README.md"
authors = [{ name = "Ivan Aphanasyev", email = "nekaktotak2@gmail.com" }]
requires-python = ">=3.13"
dependencies = [
    "datasets>=4.1.1",
    "dora-rs==0.5.0",
    "eclipse-zenoh>=1.7.2",
    "foxglove-sdk>=0.14.2",
    "gtsam-develop>=4.3a1.dev202605270118",
    "loguru>=0.7.3",
    "numpy==2.4.6",
    "opencv-contrib-python-headless>=4.13.0.92",
    "pandas>=2.3.2",
    "pyarrow==23.0",
    "pydantic>=2.12.5",
    "pyyaml>=6.0.2",
    "reactivex>=4.1.0",
    "rerun-sdk==0.35.0",
    "scipy>=1.15.3",
    "typer>=0.21.1",
]

[project.optional-dependencies]
vpr-torch = [
    "torch>=2.12.0",
    "torchvision>=0.27.0",
]

[project.scripts]
vins-rnd = "vins_rnd:main"
dataset = "dataset.cli:main"
artifact = "artifacts.cli:main"
artifacts = "artifacts.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "src/vins_rnd",
    "src/artifacts",
    "src/core",
    "src/dataset",
    "src/logger",
    "src/visualizer",
]

[tool.hatch.build.targets.sdist]
include = [
    "src/**",
    "pipeline/**",
    "tests/**",
    "examples/**",
    "README.md",
    "pyproject.toml",
    "ruff.toml",
    "ty.toml",
    "justfile",
]

[dependency-groups]
dev = [
    "datafusion>=52.0.0,<53.0.0",
    "debugpy>=1.8.17",
    "evo>=1.34.0",
    "graphviz>=0.21",
    "ipykernel>=6.30.1",
    "matplotlib>=3.10.6",
    "plotly>=6.5.0",
    "pre-commit>=4.6.0",
    "pybind11-stubgen>=2.5.1",
    "pyparsing>=3.2.5",
    "pytest>=8.4.2",
    "pytest-mock>=3.15.1",
    "pytest-watcher>=0.4.3",
    "rerun-sdk>=0.28.1",
    "ruff",
    "ty>=0.0.1a21",
    "typer>=0.26.3",
    "zenoh-cli>=0.7.0",
]


[tool.pytest-watcher]
now = false
clear = true
delay = 0.2
runner = "pytest"
runner_args = []
patterns = ["test_*.py"]
ignore_patterns = []

[tool.pytest.ini_options]
testpaths = ["pipeline", "src", "tests"]
markers = [
    "regression: dataset-based regression tests (slow)",
    "smoke: command-level smoke tests for user-facing CLI entrypoints",
]
log_cli = true
log_cli_level = "INFO"
